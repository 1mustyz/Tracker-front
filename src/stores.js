import { writable } from 'svelte/store';

export const user = writable(null);
export const vehicleStore = writable(null);
export const rLocation = writable(null);
export const rLocationTime = writable(null);